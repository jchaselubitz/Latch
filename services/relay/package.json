{
  "name": "@latch/opaque-relay",
  "version": "0.1.0",
  "description": "Opaque bounded WebSocket relay for Latch Remote Link v1",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "engines": { "node": "24.x" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types --test \"src/**/*.test.ts\"",
    "start": "node dist/main.js"
  },
  "dependencies": { "ws": "^8.18.3" },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.3"
  }
}
