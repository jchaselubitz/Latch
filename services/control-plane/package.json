{
  "name": "@latch/control-plane",
  "version": "0.1.0",
  "description": "Latch Remote Link control plane: enrollment, grants, relay admission, leases, and revocation delivery.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "files": [
    "dist",
    "migrations"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "migrate": "node dist/migrate-cli.js",
    "test": "node --experimental-strip-types --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/pg": "^8.11.10",
    "typescript": "^5.9.3"
  }
}
