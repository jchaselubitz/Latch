name: Release Latch CLI

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write
  attestations: write
  artifact-metadata: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            runner: macos-14
          - target: x86_64-apple-darwin
            runner: macos-15-intel
    runs-on: ${{ matrix.runner }}
    env:
      LATCH_RELEASE_TAG: ${{ github.ref_name }}
      LATCH_CODESIGN_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
      LATCH_NOTARY_PROFILE: latch-cli-notary

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install tmux build dependency
        run: brew install libevent

      - name: Import Developer ID certificate
        env:
          APPLE_CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          set -euo pipefail
          : "${APPLE_CERTIFICATE_BASE64:?Set the Apple signing certificate secret}"
          : "${APPLE_CERTIFICATE_PASSWORD:?Set the certificate password secret}"
          : "${KEYCHAIN_PASSWORD:?Set a temporary keychain password secret}"
          : "${LATCH_CODESIGN_IDENTITY:?Set the Developer ID Application signing identity secret}"
          : "${APPLE_ID:?Set the Apple ID notarization secret}"
          : "${APPLE_APP_SPECIFIC_PASSWORD:?Set the Apple app-specific password secret}"
          : "${APPLE_TEAM_ID:?Set the Apple Developer Team ID secret}"
          certificate_path="$RUNNER_TEMP/latch-signing.p12"
          keychain_path="$RUNNER_TEMP/latch-signing.keychain-db"
          echo "$APPLE_CERTIFICATE_BASE64" | base64 -D > "$certificate_path"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$keychain_path"
          security set-keychain-settings -lut 21600 "$keychain_path"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$keychain_path"
          security import "$certificate_path" -P "$APPLE_CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k "$keychain_path"
          security list-keychain -d user -s "$keychain_path"
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" "$keychain_path"
          xcrun notarytool store-credentials "$LATCH_NOTARY_PROFILE" \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_APP_SPECIFIC_PASSWORD" \
            --team-id "$APPLE_TEAM_ID"

      - name: Package signed CLI
        run: scripts/release-cli.sh ${{ matrix.target }}

      - name: Attest release archive
        uses: actions/attest@v4
        with:
          subject-path: dist/latch-*.zip

      - uses: actions/upload-artifact@v4
        with:
          name: latch-${{ matrix.target }}
          path: dist/
          if-no-files-found: error

  publish:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Combine and verify checksums
        run: |
          set -euo pipefail
          cat dist/latch-*.zip.sha256 > dist/checksums.txt
          (cd dist && shasum -a 256 -c checksums.txt)

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" dist/latch-*.zip dist/checksums.txt \
            --repo "$GITHUB_REPOSITORY" \
            --title "Latch $GITHUB_REF_NAME" \
            --generate-notes
