{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://latch.cooperativ.dev/schemas/remote-access/v2/create-session-request.schema.json",
  "title": "LatchCreateSessionRequestV2",
  "description": "The only caller-controlled inputs for a mobile-created session: where it starts, and optionally which hosted agent the Mac launches there instead of a standard shell.",
  "type": "object",
  "required": ["requestId", "cwd"],
  "properties": {
    "requestId": { "type": "string", "format": "uuid" },
    "cwd": { "type": "string", "minLength": 1, "maxLength": 4096, "pattern": "^/" },
    "agent": {
      "type": "string",
      "enum": ["claude", "codex"],
      "description": "Hosted agent to launch directly in the session. Absent means a standard login shell. The Mac resolves the agent's own executable and launch arguments; the caller names only the kind, and only a kind the gateway advertises in features.sessionAgents."
    }
  },
  "additionalProperties": false
}
