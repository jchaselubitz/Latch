{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://latch.cooperativ.dev/schemas/remote-access/v1/idempotency-key.schema.json",
  "title": "LatchIdempotencyKeyV1",
  "description": "The Idempotency-Key HTTP header for message and prompt-resolution submissions. It is scoped to the resolved Latch session and must be reused unchanged after an ambiguous failure.",
  "type": "string",
  "minLength": 1,
  "maxLength": 128,
  "pattern": "^[A-Za-z0-9._-]+$"
}
