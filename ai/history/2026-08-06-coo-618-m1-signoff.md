# coo:618 objective 5 — Adoption, hygiene, and M1 sign-off

## What landed

- Nesting refusal: `nesting_decision` attaches to the enclosing session when
  `LATCH_SESSION_ID` is set and creation was requested; bare `latch` / `shell`
  redirect to attach; `run` and `create` decline. `create_session` refuses to
  nest if the env is set.
- Worker exports `LATCH_SESSION_ID` into every child before spawn
  (`paths::SESSION_ID_ENV`), so nesting is detectable from inside a session.
- Auto-name and list idle sorting were already green from earlier objectives;
  reconfirmed.
- `docs/ITERM_SETUP.md`: point an iTerm profile's Command at `latch`.

## Verification

### Automated (this agent host — Linux/OrbStack, not the iTerm Mac)

| Exit criterion | Holds? | Evidence |
| --- | --- | --- |
| Open feels instant | Yes (CLI budget) | `cli_latency`; `latch --version` ~1 ms debug |
| Close window → process keeps running; `exit` ends it | Yes | `cli_e2e` |
| Reattach identical incl. alternate screen | Yes | `cli_e2e::reattach_reproduces_the_screen_identically_including_alternate_screen` |
| Second terminal: exclusive control + visible transfers | **No** | `Registry` still `todo!` (M1b / coo:617). `cli_e2e::two_terminals_…` fails with that unimplemented message. |
| `latch stop` only selected process group | Yes | `cli_e2e` |
| Killed client never affects child | Yes | `cli_e2e` / `cli_attach` |
| `latch list` useful with 30 sessions | Yes | `cli_list` within budget |
| No daemon / DB / service manager / Node in local plane | Yes | `cli_e2e` structural check; layout and boundaries |

### Interactive iTerm (dev Mac)

Not performed in this session — the agent host has no iTerm. Profile setup is
documented in `docs/ITERM_SETUP.md` for a human to apply and spot-check on the
Mac.

### Suite status

- All M1c CLI suites green: `cli_create`, `cli_attach`, `cli_json`, `cli_list`,
  `cli_latency`, `cli_nesting`, and `cli_e2e` except the registry-pinned exclusive
  control test.
- Full workspace is **not** green: M1b attachment registry / resize authority /
  bounded queues remain `todo!`, and M2 `remote_attach` has expected failures.
- `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt`, and
  `scripts/check-boundaries.sh` are clean.

## Honest gaps

1. Exclusive write (D5) is not enforced by a finished registry — criterion does
   not hold; do not relax it. Finish M1b (coo:617) before treating M1 as signed
   off.
2. iTerm-on-Mac hand check still needed by a human after installing the profile
   from `docs/ITERM_SETUP.md`.
