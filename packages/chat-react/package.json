{
  "name": "@latch/chat-react",
  "version": "0.1.0",
  "description": "React transcript and interaction components for the Latch Remote SDK.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "peerDependencies": {
    "react": ">=18"
  },
  "dependencies": {
    "@latch/client": "^0.1.0",
    "@latch/harness-schema": "^0.1.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types --test src/*.test.ts"
  }
}
